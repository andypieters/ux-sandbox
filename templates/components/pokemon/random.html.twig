<div class="col-6 col-md-4 col-lg-3 " {{ attributes }}>
    <button class="btn btn-primary" data-action="live#$render">Reroll</button>
    {{ component('pokemon:pokemon', {idOrName: this.pokemon}) }}
</div>